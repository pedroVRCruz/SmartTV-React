import styled from "styled-components";

export const Container = styled.header`
.box{
  display: flex;
  text-align: center;
}
  .logo {
    width: 48px;
    height: 48px;
  }

  .menu {
    display: flex;
    flex-direction: row;
    gap: 60px;
    font-size: 20px;
  }
`;